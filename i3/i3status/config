general {
        colors = true
        interval = 5
}

order += "battery all"
order += "tztime local"
# order += "disk /"
# order += "load"

battery all {
        format = "%status %percentage %remaining"
}

tztime local {
        format = "%Y-%m-%d %H:%M:%S"
}

# load {
#       format = "%1min"
# }

# disk "/" {
#       format = "%avail"
# }
